# frozen_string_literal: true

# ApplicationController serves as the base controller for all controllers in the application.
# It provides common functionality and configurations that are shared across all controllers.
class ApplicationController < ActionController::Base
  # protect_from_forgery with: :exceptionz

end
